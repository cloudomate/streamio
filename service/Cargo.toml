[package]
name = "streamio-service"
version = "0.1.0"
edition = "2021"

[dependencies]
streamio-types = { path = "../types" }
serde_json = "1"
serde = { version = "1", features = ["derive"] }

[build-dependencies]
embed-resource = "3"

